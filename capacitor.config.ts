import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fashionvista.firebaseanalytics',
  appName: 'firebaseAnalytic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
